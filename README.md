# 🎨 Vue 3 Form Builder

Интерактивный конструктор форм на Vue 3, где вы можете вводить JSON-схему и видеть сгенерированную форму в реальном времени.

![Vue 3](https://img.shields.io/badge/Vue.js-3.5-42b883?style=flat-square&logo=vue.js)
![Vite](https://img.shields.io/badge/Vite-5.4-646cff?style=flat-square&logo=vite)

## 🌐 Живое демо

**[👉 Попробовать конструктор форм](https://unidentifiedraccoon.github.io/artlebedev-autoform/)**

## ✨ Возможности

### 📝 Редактор схемы
- **Редактирование JSON в реальном времени** с мгновенным предварительным просмотром
- **Валидация JSON** с подробными сообщениями об ошибках
- **Форматирование JSON** одним кликом
- **Готовые примеры** схем для быстрого старта:
  - Форма регистрации
  - Форма обратной связи
  - Личная анкета
  - Оформление заказа

### 🎯 Генератор форм
Поддержка всех основных типов полей:
- `text` - текстовое поле
- `email` - поле email с валидацией
- `password` - поле пароля
- `number` - числовое поле
- `select` - выпадающий список
- `checkbox` - чекбокс
- `radio` - радиокнопки
- `textarea` - многострочное текстовое поле

### ✅ Валидация
- `required` - обязательное поле
- `minLength` / `maxLength` - минимальная/максимальная длина строки
- `min` / `max` - минимальное/максимальное значение для чисел
- `pattern` - регулярное выражение для проверки формата
- Отображение ошибок в реальном времени
- Индикатор состояния валидации формы

### 👁️ Предварительный просмотр
- **Живое отображение** формы при изменении схемы
- **JSON-превью** текущих значений формы
- **Визуальная индикация** валидности формы

## 🚀 Быстрый старт

### Установка зависимостей
```bash
npm install
```

### Запуск в режиме разработки
```bash
npm run dev
```

Приложение будет доступно по адресу [http://localhost:5173](http://localhost:5173)

### Сборка для продакшена
```bash
npm run build
```

### Предварительный просмотр продакшен-сборки
```bash
npm run preview
```

## 📋 Структура JSON-схемы

Базовая структура схемы формы:

```json
{
  "fields": [
    { "type": "text", "label": "Имя", "model": "name", "required": true },
    { "type": "email", "label": "Email", "model": "email", "required": true },
    { "type": "password", "label": "Пароль", "model": "password", "required": true, "minLength": 6 },
    { "type": "select", "label": "Роль", "model": "role", "options": ["Админ", "Пользователь"], "required": true },
    { "type": "checkbox", "label": "Согласен с условиями", "model": "terms", "required": true }
  ]
}
```

### Свойства полей

#### Общие свойства (для всех типов):
- `type` (обязательно) - тип поля
- `model` или `name` (обязательно) - уникальное имя поля для связывания данных
- `label` (обязательно) - метка/заголовок поля
- `placeholder` - текст-подсказка
- `required` - обязательное ли поле (true/false)
- `disabled` - заблокировано ли поле (true/false)
- `defaultValue` - значение по умолчанию

#### Для текстовых полей (text, email, password, textarea):
- `minLength` - минимальная длина
- `maxLength` - максимальная длина
- `pattern` - регулярное выражение для валидации

#### Для числовых полей (number):
- `min` - минимальное значение
- `max` - максимальное значение

#### Для выпадающих списков и радиокнопок (select, radio):
- `options` - массив вариантов выбора

## 📖 Примеры схем

### Простая форма (artlebedev)
```json
{
  "fields": [
    { "type": "text", "label": "Имя", "model": "name", "required": true },
    { "type": "email", "label": "Email", "model": "email", "required": true },
    { "type": "password", "label": "Пароль", "model": "password", "required": true, "minLength": 6 },
    { "type": "select", "label": "Роль", "model": "role", "options": ["Админ", "Пользователь"], "required": true },
    { "type": "checkbox", "label": "Согласен с условиями", "model": "terms", "required": true }
  ]
}
```

### Расширенная форма регистрации
```json
{
  "title": "Форма регистрации и анкета",
  "description": "Заполните данные для создания аккаунта",
  "fields": [
    {
      "type": "text",
      "name": "username",
      "label": "Имя пользователя",
      "placeholder": "Введите имя пользователя",
      "required": true,
      "minLength": 3,
      "maxLength": 20,
      "pattern": "^[a-zA-Z0-9_]+$"
    },
    {
      "type": "email",
      "name": "email",
      "label": "Email",
      "placeholder": "example@mail.com",
      "required": true
    },
    {
      "type": "password",
      "name": "password",
      "label": "Пароль",
      "placeholder": "Минимум 8 символов",
      "required": true,
      "minLength": 8
    },
    {
      "type": "number",
      "name": "age",
      "label": "Возраст",
      "required": true,
      "min": 18,
      "max": 100
    },
    {
      "type": "select",
      "name": "country",
      "label": "Страна",
      "required": true,
      "options": ["Россия", "Беларусь", "Казахстан", "Украина"]
    },
    {
      "type": "radio",
      "name": "gender",
      "label": "Пол",
      "required": true,
      "options": ["Мужской", "Женский"]
    },
    {
      "type": "checkbox",
      "name": "terms",
      "label": "Я согласен с условиями использования",
      "required": true
    }
  ]
}
```

## 🏗️ Архитектура проекта

```
src/
├── components/
│   ├── FormBuilder.vue      # Главный компонент с двумя панелями
│   └── FormGenerator.vue    # Компонент для рендеринга формы
├── App.vue                  # Корневой компонент
├── main.js                  # Точка входа
└── style.css                # Глобальные стили
```

### Компоненты

#### FormBuilder.vue
Основной компонент конструктора с двумя панелями:
- **Левая панель**: редактор JSON-схемы с примерами
- **Правая панель**: предварительный просмотр формы

#### FormGenerator.vue
Компонент для динамической генерации формы на основе JSON-схемы:
- Поддержка всех типов полей
- Валидация в реальном времени
- Отображение ошибок валидации
- Отслеживание состояния формы

## 🛠️ Технологии

- **Vue 3** - прогрессивный JavaScript фреймворк
- **Vite** - современный инструмент сборки
- **Composition API** - современный API Vue 3
- **Reactive** - реактивная система Vue 3

## 📝 Требования

- Node.js >= 20.10.0
- npm >= 10.0.0

---

Сделано с ❤️ на Vue 3
